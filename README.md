# football-predictor

Football match prediction using CatBoost on advanced engineered features
(Elo, form, expected goals, attack-vs-defense matchups).

## One-time setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
